const fruta = ["manzana", "platano", "mandarina", "frutilla", "pera"]

for (let index = 0; index < fruta.length; index++) {
    const element = fruta[index];
    console.log(fruta[index]);
    
    
}